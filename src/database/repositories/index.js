/**
 * Repositories Index
 * Corporate Warfare Discord Bot
 *
 * Barrel export for all repositories
 */

const ticketRepository = require('./ticketRepository');
const welcomeRepository = require('./welcomeRepository');
const ticketIntroRepository = require('./ticketIntroRepository');
const userRepository = require('./userRepository');

module.exports = {
    ticketRepository,
    welcomeRepository,
    ticketIntroRepository,
    userRepository,
};
